// @ts-check
import { defineConfig } from 'astro/config';

// Static Astro site. Vercel auto-detects Astro and serves the built `dist/`.
export default defineConfig({
  site: 'https://www.lightspacefiber.com',
});
